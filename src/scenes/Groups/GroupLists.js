import React, {Component} from 'react';
import Entscheidomat from "../../components/Entscheidomat";

export default class GroupLists extends Component {
	render() {
		// Show one list and let the user set the count of Groups to spread it
		return (
			<div><Entscheidomat/></div>
		)
	}
}
